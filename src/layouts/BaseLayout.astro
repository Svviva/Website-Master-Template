---
import Header from '../components/Header.astro';import Footer from '../components/Footer.astro';import '../styles/global.css';import{getEntry}from'astro:content';interface Props{title:string;lang?:'en'|'ar';description?:string;image?:string}const{title,lang='en',description='School website — replace this description with verified information.',image='/images/hero-1.jpeg'}=Astro.props;const dir=lang==='ar'?'rtl':'ltr';let settings;try{settings=await getEntry('settings','site')}catch{}const s=settings?.data;const school=lang==='ar'?(s?.schoolName_ar||'اسم المدرسة'):(s?.schoolName||'SCHOOL_NAME');const location=lang==='ar'?(s?.locationShort_ar||s?.address_ar||'الموقع'):(s?.locationShort||s?.address||'LOCATION');const wa=(s?.whatsappNumber||'').replace(/[^0-9]/g,'');const isHome=Astro.url.pathname==='/'||Astro.url.pathname==='/ar/'||Astro.url.pathname==='/ar';const canonical=Astro.site?new URL(Astro.url.pathname,Astro.site):undefined;const schema={"@context":"https://schema.org","@type":"School","name":school,"description":description,...(s?.address?{"address":{"@type":"PostalAddress","streetAddress":lang==='ar'?(s.address_ar||s.address):s.address}}:{}),...(s?.phone?{"telephone":s.phone}:{}),...(s?.email?{"email":s.email}:{}),...(canonical?{"url":canonical.toString()}: {})};
---
<!DOCTYPE html><html lang={lang} dir={dir}><head><meta charset="UTF-8"/><meta name="viewport" content="width=device-width, initial-scale=1.0"/><title>{title}</title><meta name="description" content={description}/><meta name="robots" content="index,follow"/>{canonical&&<link rel="canonical" href={canonical}/>}<meta property="og:type" content="website"/><meta property="og:title" content={title}/><meta property="og:description" content={description}/><meta property="og:image" content={image}/>{canonical&&<meta property="og:url" content={canonical}/>}<meta name="twitter:card" content="summary_large_image"/><link rel="icon" type="image/svg+xml" href="/favicon.svg"/><script type="application/ld+json" set:html={JSON.stringify(schema)}></script></head><body><a class="skip-link" href="#main-content">{lang==='ar'?'انتقل إلى المحتوى الرئيسي':'Skip to main content'}</a><Header lang={lang}/><main id="main-content"><slot/>{isHome&&s?.mapEmbedUrl&&<section class="home-location"><div class="wrap"><div class="home-location-head"><div><p>{lang==='ar'?'موقعنا':'Our location'}</p><h2>{lang==='ar'?`اعثر على ${school}`:`Find ${school}`}</h2><span>{lang==='ar'?`زورونا في ${location}.`:`Visit us in ${location}.`}</span></div><a href={lang==='ar'?'/ar/contact/':'/contact/'}>{lang==='ar'?'اتصل بنا ←':'Contact us →'}</a></div><iframe src={s.mapEmbedUrl} loading="lazy" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen title={lang==='ar'?`موقع ${school}`:`${school} location`}></iframe></div></section>}</main><Footer lang={lang}/>{wa&&<a class="floating-wa" href={`https://wa.me/${wa}?text=${encodeURIComponent(lang==='ar'?`مرحباً ${school}، أود الاستفسار عن: `:`Hello ${school}. I would like to enquire about: `)}`} target="_blank" rel="noopener" aria-label={lang==='ar'?`تواصل مع ${school} عبر واتساب`:`Contact ${school} on WhatsApp`}>WhatsApp</a>}<script is:inline>(()=>{if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const init=()=>{const candidates=[...document.querySelectorAll('#main-content > section, #main-content > article, #main-content .wrap > section')],targets=[...new Set(candidates)].filter(el=>!el.closest('.hero')&&!el.classList.contains('hero'));targets.forEach((el,i)=>{el.classList.add('page-reveal');if(i<2)el.style.setProperty('--reveal-delay',`${i*55}ms`)});if(!('IntersectionObserver'in window)){targets.forEach(el=>el.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}})},{threshold:.08,rootMargin:'0px 0px -5% 0px'});targets.forEach(el=>observer.observe(el))};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init()})();</script></body></html>
<style is:global>.skip-link{position:fixed;left:12px;top:-60px;z-index:1000;background:#fff;color:#111;padding:10px 14px;border-radius:6px}.skip-link:focus{top:12px}.floating-wa{position:fixed;right:20px;bottom:20px;z-index:45;background:#16733c;color:#fff;text-decoration:none;font-size:13px;font-weight:700;padding:12px 17px;border-radius:999px;box-shadow:0 6px 22px rgba(0,0,0,.2);transition:transform .22s ease,box-shadow .22s ease}.floating-wa:hover{transform:translateY(-2px);box-shadow:0 9px 26px rgba(0,0,0,.24)}html[dir='rtl'] .floating-wa{right:auto;left:20px}.home-location{padding:76px 0;background:#f5f7f9}.home-location-head{display:flex;align-items:flex-end;justify-content:space-between;gap:24px;margin-bottom:26px}.home-location-head p{font-size:12px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;color:var(--gold);margin-bottom:7px}.home-location-head h2{font-family:var(--font-display);font-size:clamp(28px,3vw,38px);color:var(--navy-dark);margin-bottom:7px}.home-location-head span{color:var(--slate)}.home-location-head a{color:var(--navy);font-weight:700;text-decoration:none}.home-location iframe{display:block;width:100%;height:420px;border:0;border-radius:14px;background:#e6ebef}html[dir='rtl'] .home-location-head h2{font-family:var(--font-display-ar)}.page-reveal{opacity:0;transform:translateY(18px);transition:opacity .62s ease var(--reveal-delay,0ms),transform .62s cubic-bezier(.2,.7,.25,1) var(--reveal-delay,0ms)}.page-reveal.is-visible{opacity:1;transform:none}main figure{overflow:hidden}main figure img{transition:transform .45s ease}@media(hover:hover) and (pointer:fine){main figure:hover img{transform:scale(1.018)}}@media(max-width:600px){.floating-wa{right:14px;bottom:14px;padding:11px 14px}html[dir='rtl'] .floating-wa{left:14px}.home-location{padding:56px 0}.home-location-head{align-items:flex-start;flex-direction:column}.home-location iframe{height:330px}.page-reveal{transform:translateY(12px);transition-duration:.48s}}@media(prefers-reduced-motion:reduce){.page-reveal{opacity:1!important;transform:none!important}main figure img,.floating-wa{transition:none!important}}</style>